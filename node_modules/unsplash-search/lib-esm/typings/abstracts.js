import { E_INVALID_ACCESS_KEY } from '../constants/errors';
var IUnsplashSearch = (function () {
    function IUnsplashSearch(accessKey, perPage) {
        if (!accessKey) {
            throw new Error(E_INVALID_ACCESS_KEY);
        }
        this.accessKey = accessKey;
        this.perPage = perPage;
    }
    return IUnsplashSearch;
}());
export default IUnsplashSearch;
//# sourceMappingURL=abstracts.js.map